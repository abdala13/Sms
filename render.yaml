services:
  - type: web
    name: curlflow-web
    env: docker
    plan: starter
    autoDeploy: true
    healthCheckPath: /dashboard
  - type: worker
    name: curlflow-worker
    env: docker
    dockerCommand: ./scripts/start_worker.sh
    plan: starter
