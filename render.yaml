services:
  - type: web
    name: sentinel-verify-platform
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python -m playwright install chromium
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
