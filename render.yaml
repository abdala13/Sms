services:
  - type: web
    name: curlflow-web
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true

  - type: worker
    name: curlflow-worker
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    autoDeploy: true
